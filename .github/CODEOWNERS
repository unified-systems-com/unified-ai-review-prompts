# Prompt packs are reviewer configuration — code-owned (TAP
# req-cicd-ai-review-least-privilege-5). A change to a prompt is a change to every
# review that pack performs.
*   @notgeorge @criticalsec
# @criticalsec is the second-account reviewer (two-account review, TAP req-cicd-ai-review-least-privilege-5);
# it never authenticates on the dev laptop. GitHub silently ignores unresolvable owners — verify after any access change.
